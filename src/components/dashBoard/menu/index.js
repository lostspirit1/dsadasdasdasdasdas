import React from 'react';
import './style.css';

import { useSelector } from "react-redux";
import SideBar from './menu';

const Menu = () => {
  return (
    <SideBar />
  );
}

export default Menu;